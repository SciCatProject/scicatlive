services:
  mongodb:
    image: mongo:7.0
    volumes:
      - "mongodb_data:/data/db"
      - "./config/init.js:/docker-entrypoint-initdb.d/init.js"
      - "./config/seed:/seed"

volumes:
  mongodb_data:
    driver: local
