services:
  archivemock:
    extends:
      file: ${PWD}/services/compose.dev.yaml
      service: web-app
    labels: !reset null
    volumes:
      - ${DEV_LOCAL:+${PWD}/development/}archivemock_dev:/development
    user: root

volumes:
  archivemock_dev:
    driver: local
