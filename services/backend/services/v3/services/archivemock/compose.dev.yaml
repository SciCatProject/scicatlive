services:
  archivemock:
    extends:
      file: ${PWD}/services/compose.dev.yaml
      service: web-app
    labels: !reset null
    volumes:
      - archivemock_dev:/job_mock
      - ${DEV_BBACKUP:+${PWD}/bbackup/archivemock:}/bbackup
    user: root

volumes:
  archivemock_dev:
    driver: local
