include:
  - path:
      - ./compose.base.yaml
      - ..${ELASTIC_ENABLED:+/backendv4}/compose${ELASTIC_ENABLED:+.elastic}.yaml
      - ..${LDAP_ENABLED:+/backendv4}/compose${LDAP_ENABLED:+.ldap}.yaml
