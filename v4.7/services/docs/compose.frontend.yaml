services:
  docs:
    depends_on:
      frontend:
        condition: service_healthy
    volumes:
      - type: volume
        source: frontend_dev
        target: /docs/frontend
        read_only: true
        volume:
          subpath: docs
